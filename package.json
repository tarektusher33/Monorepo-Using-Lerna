{
    "name": "root",
    "private": true,
    "version": "1.0.0",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {

        "start": "lerna run start --scope client --scope server",
        "test": "lerna run test",
        "new-version": "lerna version --conventional-commits --yes",
        "diff": "lerna diff"
    },
    "devDependencies": {
        "lerna": "^8.1.5"
    }
}